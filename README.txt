Copy these to your homedir, optionally renaming if you'd like to 
source as part of a larger profile.

For example...
cp osx/.bashrc ~/.bashrc.common
cp osx/.bash_aliases ~/.bashrc_aliases.common

In host bashrc...


